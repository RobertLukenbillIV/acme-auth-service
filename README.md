# acme-auth-service
acme-auth-service is the authentication and authorization API for the Acme platform, built with Java Spring Boot. It uses shared schemas from acme-contracts and issues JWTs for secure access by acme-tickets-service and acme-ui. Exposes OpenAPI for SDKs and consistent user, token, and error handling.
